# Compiler Seam — v1.0

The seam is the only place alphabets change. It lowers GHLL → NSC IR → NSC bytecode and optionally maps to GLLL ops.

It must preserve spans and emit receipts for every rewrite.
