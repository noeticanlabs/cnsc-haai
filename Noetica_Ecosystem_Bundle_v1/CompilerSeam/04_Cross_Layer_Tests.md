# Cross-Layer Tests — v1.0

Golden: GHLL → bytecode → VM → PDE template → GML events.
Verify determinism, receipt continuity, spans for executed ops.
Fuzz: valid random streams; no crashes; failures are receipted.
